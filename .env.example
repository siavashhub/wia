# WIA does not require an Entra app registration.
# The @microsoft/workiq CLI handles M365 authentication itself.

WIA_LOG_LEVEL=INFO

# Optional: override the Work IQ launch command (default: npx -y @microsoft/workiq mcp)
# WIA_WORKIQ_COMMAND=npx
# WIA_WORKIQ_ARGS=["-y","@microsoft/workiq","mcp"]
